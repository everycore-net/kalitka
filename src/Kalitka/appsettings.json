{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",

      "//": "The bot token is part of the request URL (/bot<token>/sendMessage).",
      "//2": "The default HttpClient logger writes full URIs, which would put the",
      "//3": "token into the log in clear text. Silence that one category.",
      "System.Net.Http.HttpClient": "Warning"
    }
  }
}
